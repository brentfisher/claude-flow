# Architecture — {{REPO_NAME}}

<!-- generated by /kb-generate — last_crawl_commit: {{COMMIT}} -->

## Overview

<!-- 2-4 sentences: what this system is, its major runtime pieces, and how they relate. -->

## Diagram

<!-- A mermaid diagram (flowchart or C4-style) reflecting the ACTUAL crawled structure —
     real module/service/package names, not placeholders. Regenerate this every run;
     don't hand-preserve a stale diagram. -->

```mermaid
flowchart TD
```

## Key Components

<!-- One subsection per major component/service/package: what it owns, what it talks to. -->

## Data Flow

<!-- How a request/job/event moves through the system end to end. -->

## External Dependencies

<!-- Frameworks, databases, third-party services — from manifests_found / frameworks_detected
     in the crawl output, plus anything discovered by reading key files. -->
