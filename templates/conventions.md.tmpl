---
type: Conventions
title: Conventions — {{REPO_NAME}}
description: <!-- one sentence: what kind of conventions this covers (style, testing, structure) -->
generated: { by: kb-generate/claude-sonnet-5, at: {{ISO_DATETIME}} }
sources:
  - id: crawl
    resource: {{GIT_REMOTE}}
    title: "{{REPO_NAME}} @ {{COMMIT}}"
---

# Conventions — {{REPO_NAME}}

<!-- Only include what's actually observable in the code — don't invent conventions
     the repo doesn't follow. Cite a real file path as evidence for each item where useful. -->

## Code Style

## Testing

## File / Module Organization

## Naming

## Notable Patterns

<!-- Anything a new contributor (or story-implementing agent) would need to match,
     e.g. error-handling style, dependency-injection pattern, state-management approach. -->
